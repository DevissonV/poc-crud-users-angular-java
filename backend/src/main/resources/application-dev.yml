spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.crud.users: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.web: DEBUG
